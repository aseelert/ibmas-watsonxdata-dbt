# 3. Metabase: consume Gold

Metabase is optional and starts independently:

```bash
bin/demo metabase
```

It connects to the same Presto catalog and provisions example Gold queries.
Use it after dbt so the business audience sees a working dashboard immediately.
